

#### ngModel
双向数据绑定一定会用到的

#### 结构型指令--*ngFor
重复器指令

####　结构型指令--*ngIf
有点像`display:none`这个属性。
```html
<div *ngIf="selectedHero">
```
当没有 selectedHero 时， ngIf 指令会从 DOM 中移除表示英雄详情的这段 HTML 。

结构型指令：它们可以修改DOM的部分结构。
``

